services:
  links:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    env_file: .env
    volumes:
      - data:/app/data
    environment:
      - DATABASE_PATH=/app/data/links.db
    restart: unless-stopped

volumes:
  data:
